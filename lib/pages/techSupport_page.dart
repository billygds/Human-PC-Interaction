import 'package:flutter/material.dart';

class TechSupportPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tech Support Page'),
      ),
      body: Center(
        child: Text('This is the tech support page!'),
      ),
    );
  }
}